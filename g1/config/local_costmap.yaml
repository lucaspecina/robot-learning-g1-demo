local_costmap:
  local_costmap:
    ros__parameters:
      use_sim_time: true
      update_frequency: 5.0
      publish_frequency: 2.0
      global_frame: odom
      robot_base_frame: base_footprint
      rolling_window: true
      width: 3
      height: 3
      resolution: 0.05
      footprint: "[[0.30, 0.30], [0.30, -0.30], [-0.30, -0.30], [-0.30, 0.30]]"
      footprint_padding: 0.01
      track_unknown_space: false
      transform_tolerance: 0.3
      plugins: ["obstacle_layer", "inflation_layer"]

      # Nav2 recomienda ObstacleLayer para un LiDAR plano y para computadoras
      # limitadas. El G1 real podrá sumar la nube 3D o nvblox sin cambiar el
      # contrato que consumirá el controlador local.
      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: true
        footprint_clearing_enabled: true
        observation_sources: scan
        scan:
          topic: /scan
          data_type: "LaserScan"
          clearing: true
          marking: true
          max_obstacle_height: 2.0
          raytrace_max_range: 3.0
          raytrace_min_range: 0.0
          obstacle_max_range: 2.5
          obstacle_min_range: 0.0

      # La inflación agrega una zona costosa alrededor del obstáculo para que
      # el planificador no roce la pared aunque la trayectoria matemática pase.
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        enabled: true
        cost_scaling_factor: 3.0
        inflation_radius: 0.55

      always_send_full_costmap: true
      introspection_mode: "disabled"
